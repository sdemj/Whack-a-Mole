# 1일차 — 파일 검사 + 즉시 수정

실행법을 아직 모르면 [0_시작하기](0_시작하기.md) 먼저 보고 오세요.

캡처 대신 **결과를 파일로 저장**해서 시작 1번, 완료 1번만 올리면 됩니다.
파일명에 자기 이름을 넣어서 누구 결과인지 구분되게 하세요.

---

**1단계 — 검사 (10분)**
- [ ] 아래처럼 실행해서 결과를 파일로 저장 (PowerShell 기준)
```
$env:JSON_OUT="1일차_시작_이름.json"
node check-files.js ./자기게임폴더
```
- [ ] 생성된 `1일차_시작_이름.json` 파일을 팀 채팅에 올리기 (캡처 안 해도 됨)

**2단계 — 수정 (남은 시간 전부)**
- [ ] `[문제]` 항목 전부 수정 (미루지 않고 오늘 끝내기)
- [ ] `[확인]` 항목은 고쳐야 하면 수정
- [ ] 고치는 중간 확인은 `JSON_OUT` 없이 그냥 `node check-files.js ./자기게임폴더`로 터미널에서만 보기 (이 단계는 파일도 캡처도 필요 없음, `[문제]` 0개 될 때까지 반복만)
- [ ] `[문제]` 0개가 되면 마지막으로 한 번 더 파일로 저장
```
$env:JSON_OUT="1일차_완료_이름.json"
node check-files.js ./자기게임폴더
```
- [ ] 생성된 `1일차_완료_이름.json` 파일을 자정 전에 팀 채팅에 올리기

---

다음: [2일차](2일차.md)
